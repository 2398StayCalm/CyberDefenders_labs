# 💻 T1595 
📌Link to the lab -> [https://cyberdefenders.org/blueteam-ctf-challenges/t1595/]

### 📓 Scenario:
Adversaries may execute active reconnaissance scans to gather information that can be used during targeting. In these scans, the adversary probes the victim infrastructure via network traffic, as opposed to other forms of reconnaissance that do not involve direct interaction.

Adversaries may perform different forms of active scanning depending on what information they seek to gather. These scans can also be performed in various ways, including using native features of network protocols such as ICMP. Information from these scans may reveal opportunities for other forms of reconnaissance (ex: Search Open Websites/Domains or Search Open Technical Databases), establishing operational resources (ex: Develop Capabilities or Obtain Capabilities), and/or initial access (ex: External Remote Services or Exploit Public-Facing Application).

### ❓Questions❓:

#### ❔  1: What is the Zero-tier network ID?

ZeroTier uses a unique 16-character hexadecimal identifier to distinguish virtual networks. Since the question specifically asked for the network ID, I searched the packet contents for references to ZeroTier-related traffic. Using Wireshark's search functionality (`Ctrl + F`) and inspecting the packet details, I located a packet containing the ZeroTier network information.

The packet details revealed the network identifier associated with the communication. Because the network ID is explicitly included within the protocol fields, it could be extracted directly from the packet without any additional calculations or filtering.

<img width="1086" height="472" alt="image" src="https://github.com/user-attachments/assets/196499de-a4cb-4427-ac94-73424b380571" />

This provided the ZeroTier network ID required to answer the question.

It worked! 🔥

#### ❔  2: What is the size of ARP packets in bytes?

To determine the size of ARP packets, I used Wireshark's **Statistics → Protocol Hierarchy** feature. This view provides a breakdown of all protocols observed in the capture, including packet counts and the total amount of traffic generated by each protocol.

By locating the ARP entry within the hierarchy, I was able to review the protocol statistics and identify the size information associated with ARP traffic. This approach is much faster than manually inspecting individual packets and provides an aggregate view of the protocol's characteristics throughout the capture.

<img width="1027" height="402" alt="image" src="https://github.com/user-attachments/assets/ae398288-50b3-4429-8837-991032e8abe0" />

The value shown in the protocol statistics provided the answer.

It worked! 🔥

#### ❔  3: What is the address that sent the most packets?

To identify the host responsible for sending the highest number of packets, I opened **Statistics → Conversations**. This feature summarizes communications between hosts and displays packet counts for each participant.

I sorted the results by the packet count columns and examined which source address generated the largest amount of traffic within the capture. Sorting the conversations makes it easy to distinguish normal hosts from systems generating significantly more activity.

<img width="1522" height="202" alt="image" src="https://github.com/user-attachments/assets/9dbc0350-6330-47d4-a7ea-acd3ff04af42" />

The address associated with the highest packet count was the correct answer.

It worked! 🔥

#### ❔  4: What is the City of the IP is connected to in the Philippines?

The question required identifying the city associated with an IP address located in the Philippines. To find this information, I inspected packets containing GeoIP metadata and expanded the IP-related fields in the packet details pane.

Wireshark was able to resolve geographic information for the IP address, including the country and city. By reviewing the GeoIP fields, I located the city value associated with the connection.

<img width="1462" height="657" alt="image" src="https://github.com/user-attachments/assets/3f591f68-0c8b-46dc-98f5-23da99e50d20" />

The city listed within the GeoIP information provided the answer.

It worked! 🔥

#### ❔  5: How many DHCP Discover messages are in the PCAPNG file?

DHCP Discover packets are broadcast messages sent by hosts attempting to locate a DHCP server on the network. To determine how many of these messages were present in the capture, I filtered the traffic to display only DHCP-related packets.

After isolating the relevant traffic, I reviewed the displayed packet count shown in the Wireshark status bar. Since the filter returned only DHCP Discover packets, the displayed count directly represented the number of Discover messages present in the capture.

<img width="1372" height="347" alt="image" src="https://github.com/user-attachments/assets/8e47052b-78cc-42de-8d3f-105ca98ebd22" />

This count provided the answer to the question.

It worked! 🔥

#### ❔  6: What is the "Target MAC address" for packet 37?

IBecause the question referenced a specific packet number, I navigated directly to packet 37 within the packet list. After selecting the packet, I expanded the ARP protocol section in the packet details pane.

ARP packets contain several important fields, including sender and target hardware addresses. The field labeled "Target MAC address" is explicitly displayed within the ARP header and can be read directly from the packet contents.

<img width="1487" height="377" alt="image" src="https://github.com/user-attachments/assets/0f125577-7b42-4205-8cb3-08322dcc959f" />

The value shown in that field was the correct answer.

It worked! 🔥

#### ❔  7: How many ARP reply packets are present in the PCAPNG file?

ARP traffic consists primarily of requests and replies. To accurately count only the reply packets, I first filtered the capture to display ARP traffic and then examined the Opcode field contained within each ARP packet.

The Opcode value identifies the packet type. ARP requests use Opcode 1, while ARP replies use Opcode 2. To isolate only reply packets, I filtered the traffic based on the reply Opcode value and reviewed the resulting packet count.

<img width="1327" height="507" alt="image" src="https://github.com/user-attachments/assets/b6d6e11f-2efe-4e76-94b2-ec315cbc2a6c" />

The number of displayed packets represented the total number of ARP replies contained in the capture.

I worked! 🔥

#### ❔  8: What is the time packet 55 sent?

Since the question referred to a specific packet number, I navigated directly to packet 55 and reviewed its details. Wireshark records timing information for every captured packet, including the exact timestamp at which it was observed.

By examining the frame information section, I located the packet timestamp and verified the recorded transmission time.

<img width="1337" height="382" alt="image" src="https://github.com/user-attachments/assets/7da75f42-429f-4a76-8361-165207e095f4" />

The timestamp displayed for packet 55 provided the answer.

It worked! 🔥

#### ❔  9: What is the range of the targeted network IP addresses?

To identify the targeted network range, I first needed to determine which subnet was receiving the majority of the scanning activity. I returned to **Statistics → Conversations** and examined the communication patterns between hosts.

Several source systems were directing traffic toward addresses within the same network range, indicating that this subnet was the focus of the reconnaissance activity. By reviewing the destination addresses and identifying the lowest and highest observed hosts within that network, I was able to determine the targeted address range.

<img width="1365" height="511" alt="image" src="https://github.com/user-attachments/assets/b18761d0-d3a3-44af-a4b3-2584ee0d9fcb" />

This established the network range targeted during the scan.

It worked! 🔥

#### ❔  10: What are the port numbers targeted by the attacker?

To identify which services were being targeted, I focused on the attacker's scanning activity and refined the displayed traffic using filters. I excluded packets containing TCP reset flags and malformed traffic to reduce noise and obtain a clearer picture of the scan.

Next, I opened **Statistics → IPv4 Statistics → Destinations and Ports** and applied the same filter. This view aggregates destination ports and displays how frequently they appear throughout the capture. Sorting the results by the count column made it easy to identify the ports that were targeted most frequently.

<img width="1320" height="507" alt="image" src="https://github.com/user-attachments/assets/1e2e4cef-28db-4f45-a31d-e27cc04f56da" />

The ports with the highest number of connection attempts were the ones targeted by the attacker.

It worked! 🔥

#### ❔  11: What is the country where the attacker is located?

After identifying the attacker's source IP address during the previous analysis, I selected one of the attack packets and inspected the IP layer details. Wireshark's GeoIP functionality provided geographic information associated with the source address.

Among the available fields were the country, region, and other location-related attributes. By reviewing the GeoIP information associated with the attacker's IP address, I was able to determine the country from which the activity originated.

<img width="1416" height="495" alt="image" src="https://github.com/user-attachments/assets/61003232-5bda-42c2-8e6f-47a1c848b55a" />

The country listed in the GeoIP data was the answer.

It worked! 🔥

#### ❔  12: What is the name of the Threat Actor with which this technique is associated?

To attribute the activity to a known threat actor, I investigated the attacker's IP address using VirusTotal. VirusTotal aggregates intelligence from multiple security vendors, researchers, and community contributors, making it a useful source for identifying infrastructure associated with malicious activity.

After searching for the IP address, I reviewed the available intelligence and examined the Community section. This section contained references connecting the observed activity to a known threat actor and provided the information required to answer the question.

<img width="1015" height="560" alt="image" src="https://github.com/user-attachments/assets/dc6faff5-0a81-48ac-bb84-d7339125afec" />

The threat actor name identified within the associated intelligence was the correct answer.

It worked! 🔥

All done! 🥇
